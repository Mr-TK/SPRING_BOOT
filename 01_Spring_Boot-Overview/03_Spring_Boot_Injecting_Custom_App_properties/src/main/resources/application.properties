#management.endpoints.web.exposure.include = health,info

# Using wildcard "*" to expose all endpoints
# Can also expose individual endpoints with comma-delimited list

# exclude individual endpoints with comma-delimited list
management.endpoints.web.exposure.exclude = health,info

#management.endpoints.web.exposure.include=*
management.info.env.enabled = true
info.app.name = Actuator-Demo-app
info.app.description = A spring-boot app to demonstrate actuator
info.app.version = 1.0.0

# =========================
# Adding Custom properties
# =========================
dev.name = mr-tk
zone.name = CodeZone